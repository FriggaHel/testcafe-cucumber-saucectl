apiVersion: v1
metadata:
  name: Testcafe+Cucumber integration
files:
  - ./features/
  - ./tests/
  # Cucumber Specifics
  - ./cucumber.js
suites:
  - name: "saucectl"
    match: "tests/.*.(spec|test).js"
image:
  base: stt-testcafe-cucumber-node
  version: latest
